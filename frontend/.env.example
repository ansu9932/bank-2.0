# API base URL (leave unset to use the Vite dev proxy at /api)
# VITE_API_BASE_URL=http://localhost:5000/api

# Cloudflare Turnstile (bot protection) — PUBLIC site key (safe to expose)
VITE_TURNSTILE_SITE_KEY="your_cloudflare_turnstile_site_key"
